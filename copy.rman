SHUTDOWN IMMEDIATE
STARTUP MOUNT
alter database archivelog;

alter database open;
LIST BACKUP;
DELETE BACKUP; 
SHUTDOWN IMMEDIATE
STARTUP MOUNT

RUN {
ALLOCATE CHANNEL d1 TYPE DISK;
BACKUP CURRENT CONTROLFILE;
BACKUP FULL FORMAT 'C:\app\cw\rman_backup\rman_%d_%U.bus' DATABASE;
}

